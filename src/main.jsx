import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import AfricanMathTimeline from './Timeline'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AfricanMathTimeline />
  </React.StrictMode>,
)
